extern {
    func printf(s: string 0...) : int
};

struct Hello {
    a = int,
    b = int
}

func initStruct(h: *struct Hello): unit {
    printf("a: %d\n", {@h}.a);
}


func main(): int {
    let h = init Hello { a = 03, b = 2 };
    initStruct(ref h);
    return 0;
}
