extern {
    func printf(s: string 0...) : int
};

struct Hello {
    a = int,
    b = int
}

func main(): int {
    let x = init Hello {a = 133, b = 2};
    printf("a + b = %d\n", {x}.b + {x}.a);
    return 0;
}
